Singleton : Create a single instance of a class and struct and use it throught the life of the server. Make the instance thread safe. 
Strategy : Decide between algorithms in runtime. 
Factory : Create instances without specifying the type of the instance 
Decorator : Extend a feature to add more behaviour to it without modifying the existing code. 
Builder : Instead of building objects using constructors with many params, add multiple getters to build the instance instead. 
Observer : Todo 
Adapter : Todo 